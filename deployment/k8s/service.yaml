apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: ai-care-expert
  labels:
    app: api-service
    component: api
spec:
  type: ClusterIP
  selector:
    app: api-service
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  sessionAffinity: None
